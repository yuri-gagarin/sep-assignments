* the queue is a simple array within the MyQueue class. an instance of the class will hold an array with the queued elements. The head of the queue will always be att array[0] and the tail will be the last elelemt.

* the enqueue and dequeue methods will add and remove elements from the queue respectively. 