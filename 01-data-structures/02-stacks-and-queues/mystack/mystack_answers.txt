* the stack is also set up as an array within the MyStack class. It will process the elements in LIFO priority. There is a pointer to the top of the stack which will be the new items/functions/whatever added to the stack. Unlike the queue, the top item will always be the tail of the stack, and will always be the first one out. I also added a simple method to display the current stack